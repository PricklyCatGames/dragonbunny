using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace AssemblyCSharp
{
public class Initializer : MonoBehaviour {

		// Use this for initialization
		void Awake ()
		{
			//var skillMainGO = Instantiate(Resources.Load("Skill_Main", typeof(GameObject)) as GameObject);
			//skillMainGO.GetComponent<skill_Main>().initializeSkillTrails();
		}
		
		// Update is called once per frame
		void Update () {
			
		}
	}
}
